
The files on this directory can be run from either the vm or the host system as long as it is a *nix machine. The scripts will try to figure out where they are and login to the VM if needed

In the VM, this directory was added to $PATH, so the full path is not needed.



